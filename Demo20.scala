object Demo20	{
	def main(args: Array[String])	{
	var floatVar = 12.456
      var intVar = 2000
      var stringVar = "Hello, Scala!"
      
      var fs = printf("-----" + "%f, ----------" + "%d, ----------" + "%s,---------", floatVar, intVar, stringVar);
      
      println(fs)
	}
	}
